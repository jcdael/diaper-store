import { ProductsClient } from './_components/products-client';

export const metadata = {
  title: 'Shop All Diapers | LittleBundle',
  description: 'Browse our full range of premium diapers. Newborn through Size 6 in Standard, Value, and Bulk packs.',
};

export default function ProductsPage() {
  return <ProductsClient />;
}
