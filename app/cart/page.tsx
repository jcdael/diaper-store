import { CartClient } from './_components/cart-client';

export const metadata = {
  title: 'Shopping Cart | LittleBundle',
  description: 'Review your cart items and proceed to checkout.',
};

export default function CartPage() {
  return <CartClient />;
}
